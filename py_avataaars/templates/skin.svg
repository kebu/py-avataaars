{% set prefix=template_name(self) %}
<g id="{{ prefix }}-{{skin_color|lower}}"
   mask="url('#{{maskID}}')"
   fill="{{skin_color.value}}">
    <g transform="translate(0.000000, 0.000000)" id="{{ prefix }}-color">
        <rect x="0" y="0" width="264" height="280"/>
    </g>
</g>
