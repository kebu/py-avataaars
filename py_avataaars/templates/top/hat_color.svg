<g id="color-hat-{{ hat_color|lower }}"
   mask="url('#{{ maskID }}')"
   fill-rule="evenodd"
   fill="{{ hat_color.value }}">
    <rect id="hat-background-color" x="0" y="0" width="264" height="280"/>
</g>
