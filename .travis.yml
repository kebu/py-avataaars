language: python
os: linux
python:
- '3.6'
- '3.7'
install: pip3 install --upgrade pip
script: "pytest test.py"
deploy:
  provider: pypi
  user: __token__
  password:
    secure: $PYPI_TOKEN
  on:
    tags: true
  distributions: sdist bdist_wheel
  skip_existing: true
