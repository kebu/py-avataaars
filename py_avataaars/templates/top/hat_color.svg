{% set prefix=template_name(self) %}
<g id="{{ prefix }}-{{ hat_color|lower }}"
   mask="url('#{{ maskID }}')"
   fill-rule="evenodd"
   fill="{{ hat_color.main_value }}">
    <rect id="{{ prefix }}-background" x="0" y="0" width="264" height="280"/>
</g>
